import React from 'react';
import ReactDOM from 'react-dom/client';
import PaymentCheckout from '../PaymentCheckoutStandalone.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PaymentCheckout />
  </React.StrictMode>
);
